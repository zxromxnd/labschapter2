# Lab 9: Logging Decorator

Decorator-based logging system with configurable log levels.

## Features

- Configurable log levels (INFO, DEBUG, ERROR)
- Logs function arguments and return values
- ISO timestamp format
- Support for sync and async functions

## Status

Work in progress...